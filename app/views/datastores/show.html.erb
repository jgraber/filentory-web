<% title(@datastore.name, "Datastores") %>

<h2><%= @datastore.name %></h2>


<%= link_to "Edit Datastore", edit_datastore_path(@datastore) %>

<%= link_to "Delete Datastore",
	datastore_path(@datastore),
	method: :delete,
	data: { confirm: "Are you sure you want to delete this datastore?"
} %>
<%= link_to "New Location", new_datastore_location_path(@datastore) %>
